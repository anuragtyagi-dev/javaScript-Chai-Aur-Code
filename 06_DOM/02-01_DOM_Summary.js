// document.getElementByClassName('class ka naam') // it will give html collections of all the elements having same class

// document.getElementById('id ka naam') // it will give html collections of element having same id name 

// coversion of html collection into array // use Array.from() method

// document.querySelector('tag') // it will select first tag in html only

// document.querySelectorAll('tag') // it will select all elements of the same tag and give a node list 

// document.querySelector('#idKaNaam') // it will select the element with particular id name i.e. provided 

// document.querySelector('.classKaNaam') // it will select the element with particular class name i.e. provided

// ************************************ STYLING
// Example: const myHeading = document.querySelector('#firstheading')
// myHeading.style.backgroundColor = 'green'
// myHeading.style.color = 'red'
// myHeading.style.padding = '10px'

// *******************************************************

